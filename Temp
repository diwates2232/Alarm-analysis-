rmdir /s /q node_modules
del package-lock.json

npm install chart.js@^4.4.2 chartjs-plugin-datalabels@^2.2.0 --save
npm install



npm install @mui/material@5.14.11 @mui/icons-material@5.14.11 @mui/system@5.14.11 @mui/x-data-grid@5.17.26


rm -rf node_modules package-lock.json
npm install



npm install --legacy-peer-deps


npm install --save-dev @babel/preset-env @babel/plugin-proposal-class-properties

import Chart from 'chart.js/auto';
const webpack = require('webpack');

module.exports = {
  babel: {
    loaderOptions: {
      ignore: [],
    },
    plugins: [
      '@babel/plugin-proposal-optional-chaining',
      '@babel/plugin-proposal-nullish-coalescing-operator',
      '@babel/plugin-proposal-class-properties', // Add this for chart.js
    ],
    presets: ['@babel/preset-env'], // Add this to support ES6+ syntax
  },
  webpack: {
    configure: (webpackConfig) => {
      const oneOfRule = webpackConfig.module.rules.find((rule) => Array.isArray(rule.oneOf));
      if (oneOfRule) {
        oneOfRule.oneOf.forEach((rule) => {
          if (
            rule.loader &&
            rule.loader.includes('babel-loader') &&
            rule.include
          ) {
            // Extend include list to transpile necessary packages
            rule.include = [
              rule.include,
              /node_modules[\\/]@mui[\\/]material/,
              /node_modules[\\/]@mui[\\/]system/,
              /node_modules[\\/]@mui[\\/]x-data-grid/,
              /node_modules[\\/]chart\.js/, // Add Chart.js to be transpiled
            ];
          }
        });
      }

      webpackConfig.resolve.alias = {
        ...(webpackConfig.resolve.alias || {}),
        process: require.resolve('process/browser'),
      };

      webpackConfig.plugins.push(
        new webpack.ProvidePlugin({
          process: 'process/browser',
        })
      );

      return webpackConfig;
    },
  },
};









Give me Updated file 

const webpack = require('webpack');

module.exports = {
  babel: {
    loaderOptions: {
      ignore: [],
    },
    plugins: [
      '@babel/plugin-proposal-optional-chaining',
      '@babel/plugin-proposal-nullish-coalescing-operator',
    ],
  },
  webpack: {
    configure: (webpackConfig) => {
      const oneOfRule = webpackConfig.module.rules.find((rule) => Array.isArray(rule.oneOf));
      if (oneOfRule) {
        oneOfRule.oneOf.forEach((rule) => {
          if (
            rule.loader &&
            rule.loader.includes('babel-loader') &&
            rule.include
          ) {
            // Add MUI ESM modules to be transpiled by Babel
            rule.include = [
              rule.include,
              /node_modules[\\/]@mui[\\/]material/,
              /node_modules[\\/]@mui[\\/]system/,
              /node_modules[\\/]@mui[\\/]x-data-grid/,
            ];
          }
        });
      }

      webpackConfig.resolve.alias = {
        ...(webpackConfig.resolve.alias || {}),
        process: require.resolve('process/browser'),
      };

      webpackConfig.plugins.push(
        new webpack.ProvidePlugin({
          process: 'process/browser',
        })
      );

      return webpackConfig;
    },
  },
};

